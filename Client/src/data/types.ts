export type Song = {
    artist: string;
    id: number;
    title: string;
}

export type tipInfoType = {
    amount: number|string;
    email: string;
    message: string;
    name: string;
}

export type User = {
    email: string;
    name: string;
    userId: string;
}